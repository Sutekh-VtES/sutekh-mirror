[bdist_rpm]
# librsvg2 pulls in the SVG gdk-pixbuf loader
requires=python-sqlobject >= 3.0
         python-ply >= 3.0
         python-configobj
         python-keyring
         librsvg2
         pygobject3
         gtk3
# Needed to build inside a mock chroot
build_requires=python-setuptools
