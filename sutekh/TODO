* Import discipline level from WhiteWolf HTML card file.
* Make import from HTML file faster (possible use lazy SQLObject writes).
* Add selection mechanisms to GUI card lists (filter, sorting, grouping).
* Make removing cards from PhysicalCard list in GUI possible.
* Create logo for wiki.
* Handle advanced versions of vampires properly.
